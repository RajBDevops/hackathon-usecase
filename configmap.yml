apiVersion: v1
kind: ConfigMap
metadata:
  name: chatgpt-config
  namespace: default
data:
  # example config keys
  APP_ENV: "production"
  APP_DEBUG: "false"
  LOG_LEVEL: "info"
  SERVICE_URL: "https://subbuhcl.com"
