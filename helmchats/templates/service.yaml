apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-app.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "my-app.name" . }}
  ports:
    - port: 80
      targetPort: {{ .Values.app.port }}
